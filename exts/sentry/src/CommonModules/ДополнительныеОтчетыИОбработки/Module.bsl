
#Область СлужебныеПроцедурыИФункции

// Выполняем подключение метода для регламентного задания
&Вместо("ВыполнитьОбработкуПоРегламентномуЗаданию")
Процедура Sentry_ВыполнитьОбработкуПоРегламентномуЗаданию(ВнешняяОбработка, ИдентификаторКоманды)
		
	Если ВнешняяОбработка = "Sentry" Тогда
		Sentry_ОбменДаннымиСервер.ОсновнаяПроцедураРаботыРегламентногоЗадания();
	Иначе
		ПродолжитьВызов(ВнешняяОбработка, ИдентификаторКоманды);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти